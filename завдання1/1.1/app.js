var firstName = prompt("Enter first name!");
var secondName = prompt("Enter second name!");

var n = Math.floor(Math.random()*100 + 1);

alert(`${firstName} та ${secondName} співпадають на: ${n} %.`);
